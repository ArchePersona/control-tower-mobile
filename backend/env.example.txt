# CONTROL TOWER Mobile Backend Environment Example
# Copy these values into your local PowerShell session or a private .env file.
# Do not commit real secrets.

MONGO_URL=mongodb://localhost:27017
DB_NAME=control_tower_mobile
JWT_SECRET=dev-secret-change-later
JWT_REFRESH_SECRET=dev-refresh-secret-change-later
